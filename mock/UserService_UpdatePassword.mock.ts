// @ts-ignore
import { Request, Response } from 'express';

export default {
  'POST /rpc/auth/UserService.UpdatePassword': (req: Request, res: Response) => {
    res.status(200).send({ code: 98, msg: '体门文般圆广受金比院也线着矿把务理。' });
  },
};
