// @ts-ignore
import { Request, Response } from 'express';

export default {
  'POST /rpc/auth/RoleService.Update': (req: Request, res: Response) => {
    res.status(200).send({ code: 64, msg: '具志养难济是部联作油劳算象总。' });
  },
};
