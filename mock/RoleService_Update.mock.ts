// @ts-ignore
import { Request, Response } from 'express';

export default {
  'POST /rpc/auth/RoleService.Update': (req: Request, res: Response) => {
    res.status(200).send({ code: 65, msg: '因再口长已少无表位其情如。' });
  },
};
