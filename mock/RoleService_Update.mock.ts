// @ts-ignore
import { Request, Response } from 'express';

export default {
  'POST /rpc/auth/RoleService.Update': (req: Request, res: Response) => {
    res.status(200).send({ code: 79, msg: '京响规素风天线四实大之装属。' });
  },
};
