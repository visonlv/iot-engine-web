// @ts-ignore
import { Request, Response } from 'express';

export default {
  'POST /rpc/auth/AppService.Update': (req: Request, res: Response) => {
    res.status(200).send({ code: 65, msg: '队有和可导眼却火习口别看位处品这。' });
  },
};
