// @ts-ignore
import { Request, Response } from 'express';

export default {
  'POST /rpc/auth/AppService.Del': (req: Request, res: Response) => {
    res.status(200).send({ code: 83, msg: '复越日离公装明林内军行造又土声组。' });
  },
};
