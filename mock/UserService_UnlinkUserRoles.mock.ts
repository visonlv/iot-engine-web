// @ts-ignore
import { Request, Response } from 'express';

export default {
  'POST /rpc/auth/UserService.UnlinkUserRoles': (req: Request, res: Response) => {
    res.status(200).send({ code: 68, msg: '织定革土路花角信图八就及没。' });
  },
};
