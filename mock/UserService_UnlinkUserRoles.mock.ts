// @ts-ignore
import { Request, Response } from 'express';

export default {
  'POST /rpc/auth/UserService.UnlinkUserRoles': (req: Request, res: Response) => {
    res.status(200).send({ code: 74, msg: '完流县查任进过史该区面定气公起积。' });
  },
};
