// @ts-ignore
import { Request, Response } from 'express';

export default {
  'POST /rpc/auth/UserService.UnlinkUserRoles': (req: Request, res: Response) => {
    res.status(200).send({ code: 64, msg: '江社为十九流总证期再存他油此难。' });
  },
};
