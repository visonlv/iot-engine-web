// @ts-ignore
import { Request, Response } from 'express';

export default {
  'POST /rpc/auth/RoleService.Del': (req: Request, res: Response) => {
    res.status(200).send({ code: 65, msg: '强深照精为始业质片北上化京在外着。' });
  },
};
