// @ts-ignore
import { Request, Response } from 'express';

export default {
  'POST /rpc/auth/RoleService.Del': (req: Request, res: Response) => {
    res.status(200).send({ code: 94, msg: '约由织斯满东容任部细行听。' });
  },
};
