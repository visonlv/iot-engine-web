// @ts-ignore
import { Request, Response } from 'express';

export default {
  'POST /rpc/auth/UserService.Del': (req: Request, res: Response) => {
    res.status(200).send({ code: 71, msg: '划活斯称我史干压议术片后与列即她。' });
  },
};
