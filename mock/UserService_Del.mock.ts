// @ts-ignore
import { Request, Response } from 'express';

export default {
  'POST /rpc/auth/UserService.Del': (req: Request, res: Response) => {
    res.status(200).send({ code: 86, msg: '却清思意车处共路增属却林几体任老。' });
  },
};
