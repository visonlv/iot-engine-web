// @ts-ignore
import { Request, Response } from 'express';

export default {
  'POST /rpc/auth/PermissionService.Del': (req: Request, res: Response) => {
    res.status(200).send({ code: 85, msg: '传口以必量车积始集我每军极增。' });
  },
};
