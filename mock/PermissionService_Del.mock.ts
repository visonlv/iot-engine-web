// @ts-ignore
import { Request, Response } from 'express';

export default {
  'POST /rpc/auth/PermissionService.Del': (req: Request, res: Response) => {
    res.status(200).send({ code: 95, msg: '布难然后北林市每省区没改准确走导。' });
  },
};
