// @ts-ignore
import { Request, Response } from 'express';

export default {
  'POST /rpc/auth/ResourceService.Del': (req: Request, res: Response) => {
    res.status(200).send({ code: 69, msg: '京用又世支话家更八火为严月共第总。' });
  },
};
