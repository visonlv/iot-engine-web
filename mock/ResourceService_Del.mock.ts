// @ts-ignore
import { Request, Response } from 'express';

export default {
  'POST /rpc/auth/ResourceService.Del': (req: Request, res: Response) => {
    res.status(200).send({ code: 85, msg: '他金候主这先王大九复市程二此回。' });
  },
};
