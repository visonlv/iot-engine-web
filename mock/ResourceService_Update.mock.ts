// @ts-ignore
import { Request, Response } from 'express';

export default {
  'POST /rpc/auth/ResourceService.Update': (req: Request, res: Response) => {
    res.status(200).send({ code: 61, msg: '矿布到改酸教日非问由反科气。' });
  },
};
