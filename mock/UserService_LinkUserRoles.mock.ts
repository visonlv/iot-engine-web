// @ts-ignore
import { Request, Response } from 'express';

export default {
  'POST /rpc/auth/UserService.LinkUserRoles': (req: Request, res: Response) => {
    res.status(200).send({ code: 69, msg: '器米制约角候照没四多根构组。' });
  },
};
