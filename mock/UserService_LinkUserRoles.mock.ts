// @ts-ignore
import { Request, Response } from 'express';

export default {
  'POST /rpc/auth/UserService.LinkUserRoles': (req: Request, res: Response) => {
    res.status(200).send({ code: 77, msg: '类严相业也无又任更压定温我向连。' });
  },
};
